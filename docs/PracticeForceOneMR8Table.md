---
title: "PracticeForceOneMR8Table"
---

# PracticeForceOne MR8 Table — Critical Competitive Gap-Closure Plan

**Last reviewed: 2026-07-24**

**Opened:** 2026-06-26 · **Status:** 🏁 **CLOSED 2026-07-07 (founder direction)** — 16/16 picks engineering-complete and production-verified (engineering closed 2026-07-05 on build 1658; DLP7 re-validated 20/20 on 1681; plaintext debt retired with DB proof on 1683); the 11-doc commercial-launch suite is published; **every open item is pushed to [MR9](<PracticeForceOneMR9Table.md>) (§8 M9-80…88 + stitched IDs) — MR9 is the live backlog, this table is now history + the activation-detail reference** · **Owners:** **AgentMR81** (compliance/security/clinical/data) + **AgentMR82** (revenue/engagement/reporting) · **Source:** [PracticeForceOneECWDeepDive](<PracticeForceOneECWDeepDive.md>) · **Companion:** [MR7](<PracticeForceOneMR7Table.md>) (security/quality hardening — declared engineering-complete 2026-07-07, maintenance mode).

> **How to use this page (founder).** This is a **pick-list**. Each enhancement is written in plain English — *what you (and your practices) actually get* — with a checkbox to mark the ones you want, a rough size, and what's needed to start it. Skip the engineering detail unless you want it; the "What it takes" line is the short version. Mark **☑** the rows you want and I'll pull them into an active build plan.
>
> **The big idea:** PracticeForceOne wins by being the **money-recovering, click-saving billing-and-workflow brain** — not by copying everything eClinicalWorks does. So the list is ordered by *what makes that real first*. The first three items (M8-1/2/3) unlock the most value for the least work, because the smart-billing engine is **already built** — it's just running on pretend data until you connect one billing network.

**Selection key:** ☐ = not chosen · ☑ = I want this · 💰 = directly makes/recovers money · ⭐ = signature differentiator · 🧱 = table-stakes (needed to be taken seriously) · 🤝 = better to partner/buy than build · ⏸ = recommend defer.
**Size:** S = days · M = weeks · L = months · XL = major program.

---

## 📊 Where the 16 picks stand — WHAT'S LEFT (updated 2026-07-07 12:45Z, live build 1683; engineering CLOSED 2026-07-05)

**Legend:** ✅ done/live · 🔵 in progress · ⛔ blocked on **founder** · 🤝 blocked on another **agent** · ⬜ not started.

### AgentMR81 lane (compliance / security / clinical / data) — *authoritative*
| # | Item | Status | What's left |
|---|---|---|---|
| **M8-54** | Drug-interaction safety screening | ✅ **DONE** (live, flag-OFF) | nothing — founder flips `DRUG_INTERACTION_CHECK_ENABLED` to turn on |
| **M8-24** | USCDI export ($everything + FHIR resources) | ✅ **DONE** (live) | nothing |
| **M8-33** | e-Rx refill (RxRenewalRequest) | ✅ **DONE** (live) | minor: make the multi-write transaction atomic (follow-up, tracked) |
| **M8-23** | Durable audit trail | ✅ **DONE** | Full migration complete — **every** best-effort `batchCreateAUDIT_LOG` side-write in `util/` now on `AuditWriter.write()`; **zero `reply-unchecked` AUDIT_LOG remain**. Recovered 7 files the destructive sync `e96a24cee` reverted (`f3bfab02c`) + final 2 AuthResetPassword/Denials (`e93a6d5b3`). Deny-on-failure sites (BreakGlass/AuditDisposition/RuntimeReportSnapshots) correctly kept explicit reply-check. ⚠️ *Retention follow-up found 2026-07-06 (AgentDLP): the `[AUDIT-FALLBACK]` safety net preserves every event (93 entries survived the 07-06 DB-connection squeeze — the design working), but those rows live ONLY in Cloud Logging with ~30-day default retention vs the 6-year HIPAA horizon, and there's no replay into AUDIT_LOG. Fix = a log sink with long retention (⛔founder, minutes in the console) and/or a reconciliation job (code).* |
| **M8-21** | RBAC (role-based permissions) | ✅ **DONE & LIVE** | **Founder approved the permission matrix 2026-06-30.** `RBAC_ENFORCEMENT_ENABLED=true` **and** `RBAC_UI_GATING=true` both live in `cloudbuild.yaml` — server enforcement + UI button-gating active. Button-tags restored after the sync clobbered 3 pages (`bef1f38db`); `rbac.js` verified fail-open (a permission-fetch hiccup hides nothing). Phases 1–5 complete. |
| **M8-22** | Extra encryption of sensitive data | ✅ **LIVE & VERIFIED (2026-07-05, build 1658; re-proven at-rest 2026-07-06 by direct DB read — all of that day's writes 100% `enc:v1:`: 28/28 insurance rows, 14/14 MFA secrets)** · ✅ *follow-up EXECUTED (AgentMR8, 2026-07-07 12:39Z on build 1683): plaintext debt is RETIRED — dry-run 178 candidates (96 subscriber_ids + 69 MFA secrets + 13 patient account/ssn) → live run 178/178 updated, 0 failed → dry-run 0 → direct-DB proof: **plaintext = 0 in every encrypted column**, blind index present on 129/129 subscriber_ids + 16/16 account_numbers; durable AUDIT_LOG row with integrity hash. Every value in the M8-22 scope is now ciphertext at rest.* — raw-DB proof: new `subscriber_id` rows are `enc:v1:` AES-256-GCM ciphertext + `bx1:` blind indexes (identical values → different ciphertext, same bidx — searchable-encryption property confirmed); pre-flip plaintext rows coexist via dual-read; **suite 480/480 with encryption on** (Insurance 28/28 = decrypt round-trip; Claim Lifecycle 79/79 = no ciphertext in EDI). Key in Secret Manager; flag in both deploy configs. *(prior build detail follows)* | **Implementation complete 2026-07-04 (flag-OFF dormant).** Migration 029 **applied + verified on staging** (widened cols + 3 indexed `*_bidx`; EligibilityDetailView drop/recreate handled); the 3 tables **regenerated GREEN** (jac2026.1, 99 Cruds, 0 regression signatures) with a NOT-NULL bind audit defusing the JAC12 trap (1 gap found + fixed); **encrypt/decrypt/blind-index wired**: PatientInsurancesRoutes (subscriber_id+bidx, group_number), PatientRoutes (ssn_last_four+bidx, account_number+bidx), ProvidersRoutes (dea_number). AES-256-GCM dual-read = zero behavior change until flip; **suite 480/480 against live post-DDL**. **To ACTIVATE:** founder/CICD provisions `field-encryption-key` (Secret Manager) → env `FIELD_ENCRYPTION_KEY` + `FIELD_ENCRYPTION_ENABLED=true`; AgentMR82 adds bidx-equality/decrypt in Eligibility/Era readers at flip (agreed 2026-06-26). MFA secret (P1) already live-dormant. |
| **M8-35** | Imaging / radiology orders | ✅ **DONE** (re-completed 2026-07-03) | Sync had reverted the `ClinicalOrdersRoutes` wiring; **restored via generated CRUD** on AgentJAC's regenerated JEO (no raw-SQL): POST persists modality/body_part/laterality/contrast/reason; GET surfaces all 6 imaging fields; PUT updates them incl. `report_impression` on the result action. Tenant isolation + API contract preserved; ClinicalOrdersRoutes + full router compile clean; **behavioral UAT scenario added** (`uat-characterize.ps1` — imaging metadata round-trip + report_impression + org-B isolation). |
| **M8-12** | Backup + practiced restore drill | ⛔ **blocked on founder** · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-31** | pick a drill window. *Correction (07-06 audit): the M7-71 OPERATIONS.md runbook covers deploy/ops but has NO backup/restore section — the DR runbook gets written+proven during the drill itself. Prereq: flip the R1 backup switches first (Cloud SQL automated backups + PITR are still OFF, re-verified 07-07).* |

### AgentMR82 lane (revenue / engagement / reporting) — *code-done per the 2026-07-05 close (zero open engineering); activation gates below*
| # | Item | Status |
|---|---|---|
| **M8-4** | Live AI denial-recovery loop | ✅ **code-done** (`DenialAutopilotExecutor` orchestrator) — goes live when M8-1/2/3 connect + AI flags flip · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-81** |
| **M8-41** | Patient self-scheduling | ✅ **code-done** (portal self-book endpoint) — booking rules/guardrails are the founder's call · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-27** |
| **M8-50** | AI ambient scribe | ✅ build-vs-buy evaluation delivered (partner decision) · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-85** |
| **M8-55** | Custom report builder | ✅ build-vs-buy evaluation delivered · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-85** |
| **M8-2** | Real-time eligibility | ✅ seam built · ⛔ **vendor-gated** (rides M8-1 clearinghouse) · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-24 (+ hard gate M9-86)** |
| **M8-3** | ERA auto-posting | ✅ seam built · ⛔ **vendor-gated** (rides M8-1) · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-24 (+ hard gate M9-86)** |
| **M8-6** | Patient statements | ✅ seam built · ⛔ **vendor-gated** (statement/mail vendor) · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-26** |
| **M8-40** | Appointment reminders | ✅ seam built · ⛔ **vendor-gated** (Twilio/SendGrid) · **⏩ pushed to [MR9](<PracticeForceOneMR9Table.md>) → M9-27** |

### What needs YOU (founder) to unblock — ⏩ ALL PUSHED TO [MR9](<PracticeForceOneMR9Table.md>) (2026-07-07, founder direction — MR9 is the live backlog; this list stays as the MR8-side record)
1. ~~**M8-21 Phase 5** — flip `RBAC_UI_GATING`~~ ✅ **DONE** — you approved the matrix (2026-06-30); RBAC is **live** (enforcement + UI gating both on).
2. ~~**M8-22** — pick the encryption approach / provision the key~~ ✅ **DONE & LIVE** (2026-07-05, build 1658) — ciphertext + blind indexes verified at rest; suite 480/480 with encryption on.
3. **M8-12** — pick a disaster-recovery drill window. *(flip the R1 backup switches first; the DR runbook is written+proven during the drill — the audit found OPERATIONS.md doesn't cover restore yet)* → **M9-31**
4. **Wave-1 revenue (M8-2/3/6/40 + the clearinghouse M8-1, payments M8-5)** — **vendor contracts**; every seam is built but can't go live until a vendor is signed. **M8-1 is the keystone** — until a clearinghouse is signed, the AI billing engine still practices on simulated data. → **M9-80 (sign) + M9-24/26/27 (activate)**
5. **M8-20** — ONC certification: your cost/timeline scoping call (separate XL track; start early if any target buyer demands the badge). → **M9-84**
6. **The 3 platform switches from the readiness audit (R1–R3)** — still open as of 2026-07-07: ① enable Cloud SQL automated backups + PITR (backups are OFF at every layer), ② flip the staging posture out of prod (`JAC_ENV` still reports `staging` on live 1681), ③ one uptime alert. Full context: [Production Risk Register](<PracticeForceOneProductionRiskRegister.md>). *New small one:* ④ an audit-fallback **log sink with 6-year retention** (see the M8-23 row note). → **M9-31 / M9-1 / M9-4** (executable: [Founder Action Checklists](<PracticeForceOneFounderActionChecklists.md>))

> ~~**⚠ Current fleet blocker:** JAC12 NOT-NULL create-guard regression…~~ ✅ **RESOLVED** — AgentJAC's JAC13 sparse-create fix landed and was runtime-proven (2026-07-04); the follow-on SEV1-1651 packaging gap was hotfixed same-day and both are validated in production (DLP7 20/20 on build 1664, 2026-07-06). Signup/appeals emergency workarounds retired; no fleet blocker gates any MR8 item.

## 🏁 MR81 Close-out Assessment (AgentMR81, 2026-07-03, build 1626) — *historical; superseded by the 07-04/07-05 log entries (M8-22 went LIVE, the JAC12 blocker cleared)*

**Completed (MR81 — 7 of 8):** M8-12 (DR runbook) · M8-21 (RBAC, live) · M8-23 (durable audit) · M8-24 (FHIR export) · M8-33 (e-Rx refill) · M8-35 (imaging, re-completed) · M8-54 (drug interactions). M8-22 P1 (MFA-secret encryption) also live-dormant.

**Remaining (not MR81-actionable):**
- **M8-22 P2** (insurance/patient/provider field encryption) — foundation built (`FieldCipher.blindIndex` + `migration 029`); completion (JEO regen for widened + `*_bidx` columns → route wiring → flag flip + key) is **blocked on AgentJAC's data-layer regen** clearing the JAC12 NOT-NULL-guard regression. A fresh regen of these tables *now* would inherit the broken guard, so it correctly waits.
- **M8-12 drill execution** — runbook shipped; the actual restore rehearsal needs a ⛔founder maintenance window.

**Production-readiness (MR81 features):**
- **RBAC:** universal `JacHttp → RbacGate` enforcement (not the old ~8-endpoint limit), fail-open, `RBAC_KILL_SWITCH`, shadow-mode, 29/29 route-map tests. Live.
- **Audit:** `AuditWriter` — reply-checked, collision-resistant IDs, SHA-256 integrity, Cloud-Logging fallback; **zero** silently-dropped audits.
- **Encryption:** AES-256-GCM + HMAC blind-index, fail-safe (never breaks a write), dual-read (migration-free flip). P1 live-dormant; P2 built.
- **Tenant isolation:** verified across MR81 (RBAC route-map, M8-35 org-B UAT, FHIR org-scope). **Feature flags:** all MR81 toggles functional + default-safe.
- **Suite: ✅ 480/480 — FULLY GREEN on live build 1633 (2026-07-03).** All 19 workflow groups pass: Claim Lifecycle 79/79, Payments 53/53, Eligibility 50/50, Tenant Scope 49/49, Denials 26/26, etc. The recovery arc: 441/480 (empty-id EDI bugs) → 459 (signup workaround) → 478 (appeal workaround) → **480** (ERA no-arg-getter fix). Signup + appeals remain on the JAC12 emergency workarounds until the regen lands, but every user-facing workflow verifies green.

**Recommendation for MR8 close-out:** the single highest-leverage action for MR8 *and* the whole app is **landing AgentJAC's JAC12 generator fix via a full data-layer regen + redeploy** — it unblocks M8-22 completion, retires the 3 signup/appeal emergency workarounds, and returns signup/appeals/ERA to green (→ ~480/480). After that regen, MR81 finishes M8-22 in one small pass; MR8 is then code-complete pending only vendor contracts (M8-2/3), the founder DR-drill window (M8-12), and ONC cert (M8-20, separate track). **AgentMR81 has no remaining unblocked engineering work; awaiting the regen (then M8-22) or reassignment.**

> ## 📌 MR8 status log (newest first)
> **2026-07-07 (final) — 🏁 MR8 FORMALLY CLOSED (founder direction; AgentMR8).** The closure the mission recommended is executed: 16/16 picks engineering-complete and production-verified; the plaintext debt retired with direct-DB proof (build 1683); the 11-document commercial-launch suite published ([Executive Closeout](<PracticeForceOneMR8ExecutiveCloseout.md>) is the entry point); every open item carried to [MR9](<PracticeForceOneMR9Table.md>) with traceable IDs (§8 M9-80…88 + [M8-x] stitching), now owned by **AgentMR9**. What MR8 leaves behind: a production system whose only path to first revenue is the M9-80 clearinghouse signature, three platform switches, and the activation playbook it wrote for exactly that day. The AgentMR8 lane retires to history-reference duty; activation execution belongs to MR9. **This table is frozen except for historical corrections.**
> **2026-07-07 (evening) — ⏩ ALL OPEN MR8 ITEMS PUSHED TO [MR9](<PracticeForceOneMR9Table.md>) (founder direction; AgentMR8).** The MR9Table (opened by AgentMR7 at MR7 close) now carries the MR8 remainder explicitly: new **§8** rows M9-80…88 (M8-1 signing → M9-80 · M8-4 AI flip → M9-81 · M8-10 pilot → M9-82 · M8-30 e-Rx → M9-83 · M8-20 ONC → M9-84 · M8-50/55 partner picks → M9-85 · R13 bidx hard-gate → M9-86 · R14 support blanks → M9-87 · R9 HIPAA P1 deferred → M9-88) + [M8-x] IDs stitched onto the overlapping vendor/ops rows (M9-24/26/27/31/33/39). Every open row and founder-queue line in THIS table is stamped with its M9 destination. **MR9 is the live backlog; this table remains the MR8 history + activation-detail reference.**
> **2026-07-07 (13:1xZ) — COMMERCIAL-LAUNCH DOC SET COMPLETE; MR8 COMPLETION CONDITION MET — FORMAL CLOSURE RECOMMENDED (AgentMR8, Revenue Activation & Commercial Launch mission).** Gap-mapped the launch mission against the existing 8-doc suite and filled only what was genuinely missing (nothing re-run): **① [Revenue Activation Plan](<PracticeForceOneRevenueActivationPlan.md>)** — the sequenced founder view of M8-1→2→3→4 + 5/6/40 with a **code-grounded configuration inventory** (exact live env vars incl. the `SMTP_*`/`ANTHROPIC_API_KEY`/`WEBHOOK_SECRET` set; correction: M8-4's flip is the per-practice **`ai_autopilot`** flag in the AI-flags console — the old `MR3_AUTOPILOT_ENABLED` env is retired), the two hard gates (R13 bidx — now load-bearing after the backfill — and R1/R2/R3 before real credentials), uniform deploy/validate/rollback rules. **② [Competitive Landscape](<PracticeForceOneCompetitiveLandscape.md>)** — vs eCW/Athenahealth/AdvancedMD/NextGen/DrChrono; positioning lines, honest table-stakes register, MR9 recommendations aligned with the roadmap. **③ [Support Procedures Kit](<PracticeForceOneSupportProceduresKit.md>)** — R14 reduced to two founder blanks (inbox address + stated hours): severity ladder, honest SLA v1 (99.5%, no credits), 10-minute triage runbook keyed to the fleet incident classes, comms templates. **④** Delta banners on the [Readiness Report](<PracticeForceOneMR8ProductionReadinessReport.md>) + [Executive Closeout](<PracticeForceOneMR8ExecutiveCloseout.md>) (encryption 100% at rest; R6 retired by MR7's ancestor-check; R15 added). **Completion condition:** every remaining line item now requires a vendor contract, a founder decision, customer onboarding, or a certification body — **recommend formally closing MR8 and transitioning product development to [MR9](<PracticeForceOneMR9Roadmap.md>).** MR8 lane goes to activation-watch (this table stays the activation checklist).
> **2026-07-07 (12:39Z) — PLAINTEXT DEBT RETIRED: encryption backfill EXECUTED on live build 1683 (AgentMR8).** `ac6b1886e` rode the deploy; full run order executed with evidence at every step: **dry-run** found 178 candidates (96 `patient_insurances.subscriber_id`, 69 `user_mfa.secret`, 13 `patients` account/ssn stragglers, 0 providers — matching the 07-06 audit) → **live run 178/178 updated, 0 failed, 0 verify-skipped** (1.2s) → **dry-run again: 0 candidates in all four tables** → **independent direct-DB read: plaintext = 0 in all six encrypted columns**, `bx1:` blind index present on every encrypted searchable value (129/129 subscriber_ids, 16/16 account_numbers), `patients.ssn_last_four` empty in staging (nothing to encrypt) → durable `AUDIT_LOG` row (`encryption_backfill`, SHA-256 integrity hash, counts-only notes). **M8-22 is now complete in the strongest sense: every value in scope is ciphertext at rest, including all pre-flip legacy rows.** R13 (blind-index readers at M8-2/3 activation) becomes load-bearing now that no plaintext-equality fallback exists — it's already a hard checklist item in both activation packages. Also re-verified same hour (gcloud reauthed): **Cloud SQL backups still OFF** (`enabled=False`, no PITR) — R1 remains the top founder switch.
> **2026-07-07 (later) — ENCRYPTION BACKFILL BUILT (AgentMR8, `ac6b1886e`).** The plaintext-debt follow-up from the morning entry is now code-complete: `POST /api/admin/batch-utility/encryption-backfill/run` (admin role; **dry-run by default**, `?dryRun=false` to write, `?limit=N` budget) re-encrypts every legacy plaintext value in the M8-22 scope via generated CRUD — refuses when the cipher is inactive, verifies `decrypt(encrypt(x))==x` per value before writing, computes blind indexes for the searchable fields, reply-checks every update, and audits live runs (counts only, never values). phase0 274/274 + all four guardrail ratchets PASS. Rides the next deploy; run order = dry-run (inspect counts ≈ 96+69) → live run → re-run dry-run (expect candidates 0) → AgentDLP's direct-DB read for independent proof. Also shipped en route: phase0 failure output now prints javac **error lines first** (the warning flood had been hiding every real compile error — found while landing this), the risk register gained **R15** (audit-fallback retention, founder one-paster included) and a rewritten **R6** (1672 rollback = the risk materializing), and BUILD-STATUS got a 2026-07-07 correction entry (1672 was a rollback, live = 1681).
> **2026-07-07 — ACTIVATION-WATCH UPDATE (AgentMR8, live build 1681).** Everything in the MR8 surface is healthy on live; the open list is unchanged in kind but has two new follow-ups. **① MR8 cargo shipped:** the `2a6ccc0f4` PatientRoutes update-path SQL/PHI-leak fix went live on build 1671 and is serving on 1681 (health provenance `6e58ab8ad` descends from it). En route it survived a real incident: build 1672 briefly promoted OLDER content over 1671 (a stale cloud candidate passed its gate and promoted on gate-green alone) — caught and restored same-day; that is exactly the **R6 provenance risk** from the risk register materializing, and MR7 (now holding CI/CD) owns the durable fix (promote-time ancestor check). **② M8-22 encryption re-proven at rest** (AgentDLP, direct DB read — the strongest form of proof, immune to dual-read masking): clean 07-05 cutover, all 07-06 writes 100% encrypted (28/28 insurance rows, 14/14 MFA secrets incl. every portal-MFA enrollment). *New follow-up in my lane:* **backfill-encrypt the pre-flip plaintext debt** (~96 legacy subscriber_ids + 69 user_mfa secrets from 06-10→06-29) via an admin-only server-side pass using the production FieldCipher — hygiene, not urgent (dual-read reads them fine; they're just not yet ciphertext at rest). **③ M8-23 retention gap found** (AgentDLP): the audit fallback stream is preserved but log-only, in Cloud Logging's ~30-day default retention vs the 6-year HIPAA horizon — needs a log sink (⛔founder, minutes) and/or a reconciliation job into AUDIT_LOG (code). **④ Adjacent win for the buyer checklist:** portal MFA (M7-18) live-validated 14/14 end-to-end, secrets encrypted at rest; DLP7 re-certified **20/20 on build 1681 today**. **⑤ The founder switch queue R1–R3 is still open** (live still reports `environment: staging`) — it remains the only gate between "engineering complete" and "first-customer ready"; M8-1 clearinghouse contract remains the revenue keystone.
> **2026-07-06 (later) — COMMERCIAL-READINESS SUITE PUBLISHED (AgentMR8, Activation & Certification Readiness mission).** Eight founder deliverables now live on this wiki: [Executive Closeout](<PracticeForceOneMR8ExecutiveCloseout.md>) (start here) · [Production Readiness Report](<PracticeForceOneMR8ProductionReadinessReport.md>) · [Production Risk Register](<PracticeForceOneProductionRiskRegister.md>) · [Vendor Activation Playbook](<PracticeForceOneVendorActivationPlaybook.md>) · [ONC Certification Readiness](<PracticeForceOneONCCertificationReadiness.md>) · [First Customer Go-Live Checklist](<PracticeForceOneFirstCustomerGoLiveChecklist.md>) · [Operations Runbook Review](<PracticeForceOneOperationsRunbookReview.md>) · [MR9 Roadmap](<PracticeForceOneMR9Roadmap.md>). Audit headline: features ready; platform needs 3 switches (backups OFF at every layer — schedule_enabled=false, Cloud SQL automated=False, PITR off, last backup 07-02; staging posture ON in prod; zero alerting). One genuine defect repaired (`2a6ccc0f4` PatientRoutes update-path SQL/PHI leak) + 3 stale ISSUES closed. **MR8 declared commercially ready conditional on R1–R3; recommend MR9 transition.**
> **2026-07-06 — TABLE CLOSED OUT AT ENGINEERING-COMPLETE (AgentMR8, build 1665).** Post-close validation is in: **DLP7 MR7/MR8 verification journey 20/20 GREEN on live build 1664** (portal registration, approve→activate, appointment flow, encounter→coding→claim create, ERA denial/payment automation — all exercised on top of the MR8 surface); fleet has since advanced to 1665. The JAC12/JAC13 fleet-blocker saga is fully resolved and validated in production; the raw-SQL ratchet is now ENFORCED fleet-wide (new raw `prepareStatement` fails release-preflight). **Scorecard: 16/16 picks engineering-complete** — MR81 8/8 live, MR82 4/4 buildable code-done + 4/4 vendor-gated seams ready. **What remains is not code:** ① M8-1 clearinghouse contract (keystone — unlocks M8-2/3/4 live revenue), ② statement/messaging/payment vendors (M8-6/40/5), ③ founder DR-drill window (M8-12), ④ ONC scoping (M8-20). The table stays as the activation checklist; no agent lane holds open MR8 work.
> **2026-07-05 — M8-22 LIVE & VERIFIED (AgentMR81, build 1658): field encryption is ON in production.** Founder directed "complete the encryption task": **(1)** full reader-decrypt sweep — all 5 EDI generators (patient ssn + insurance member/group/subscriber maps), FhirPatientRoutes Coverage, PatientRoutes card reads (PracticePayers confirmed out-of-scope; zero plaintext equality lookups existed). **(2)** 32-byte key generated → Secret Manager `field-encryption-key` (never logged; IAM mirrored to the 3 runtime SAs). **(3)** `FIELD_ENCRYPTION_ENABLED=true` + key mount in BOTH deploy configs. **(4)** Held off force-flipping 1653 (its image predated the reader sweep by 9 min — flag and code must travel together); the next cloud build carried both. **(5) Verified on 1658:** raw DB shows `enc:v1:` ciphertext + `bx1:` blind indexes on new rows (same value twice → different ciphertext, same bidx), pre-flip plaintext coexists (dual-read), **suite 480/480 with encryption on**. En route, the 1651 `JacSparseCreateHelper` SEV1 (all creates down; NOT MR8 — a JAC13 packaging gap) was red-flagged with Cloud-Logging evidence and cleared by the hermetic cloud rebuild; the M8-23 `[AUDIT-FALLBACK]` stream captured every audit event through the outage (zero audit loss — the durable-audit design proving itself). **MR8 remaining: vendor contracts (M8-1/2/3/5/6/40), founder DR-drill window (M8-12), ONC scoping (M8-20). Zero engineering.**
> **2026-07-04 — M8-22 IMPLEMENTATION COMPLETE (AgentMR81) → MR81 lane 8/8 code-done; MR8 engineering is FINISHED.** Founder directed "continue until MR8 is done," so the regen wait was ended by doing it directly: **(1)** migration 029 applied to staging via JDBC (psql absent locally) with `lock_timeout`+retry — found and terminated an **18-min idle-in-transaction pool connection** (a real app-pool bug: `SELECT 1` keepalives inside a never-closed transaction; flagged) plus an orphaned first ALTER wedging the lock queue; EligibilityDetailView drop/recreate handled (Postgres refuses ALTER TYPE on viewed columns); all 8 columns verified. **(2)** Canonical-XML widen + `*_BIDX` fields → full `generate-jac2026` regen **GREEN** (99 Cruds, all new-format, 0 blank-typed) → phase0 + full router compile green. **(3)** NOT-NULL bind audit across all 5 create-callers of the 3 regened tables (JAC12-trap defusal): one gap found + fixed (StagingSeedDashboardRoutes patient-seed `IS_ACTIVE`). **(4)** Encrypt/decrypt/blind-index wired in the 3 owning modules (dual-read, flag-OFF inert). **(5) Suite 480/480 against live post-DDL** (Eligibility 50/50 proves the recreated view). Remaining to *activate* (not build): key provisioning (⛔founder/CICD) + MR82's reader-side bidx at flip. **MR8 now has zero open engineering: everything left is vendor contracts (M8-1/2/3/5/6/40 seams ready), founder windows (M8-12 drill, key flip), or cert scoping (M8-20).**
> **2026-07-03 — M8-35 RE-COMPLETED (AgentMR81).** Founder reassigned M8-35 to MR81. Restored the `ClinicalOrdersRoutes` imaging wiring the destructive sync had reverted — now via **generated CRUD** on AgentJAC's regenerated CLINICAL_ORDERS JEO (`9dd1f2f94`), replacing the original SANCTIONED-RAW approach: POST persists modality/body_part/laterality/contrast/reason on imaging orders (CONTRAST_REQUIRED explicit over the 500-safe default); GET surfaces all 6; PUT dirty-flag-updates them incl. `report_impression` on the result action. Generated-CRUD only, tenant isolation + API contract preserved, non-imaging orders unaffected. ClinicalOrdersRoutes + full router compile clean; **added a behavioral UAT scenario** in `uat-characterize.ps1` (imaging create → metadata round-trip → report_impression on result → org-B write-isolation). **M8-35 ✅ DONE → MR81 now 7 of 8; only M8-22 remains (at the fleet-regen boundary).**
> **2026-07-03 — MR81 status refresh (AgentMR81, build 1626).** **M8-23 fully DONE** — completed the AuditWriter migration: every `reply-unchecked` `batchCreateAUDIT_LOG` in `util/` is now durable (`AuditWriter.write()`), zero remain — incl. recovering **7 files the destructive cross-machine sync `e96a24cee` reverted** (`f3bfab02c`) + the final 2 (`e93a6d5b3`). **M8-21 DONE & LIVE** — founder approved the permission matrix 2026-06-30; `RBAC_ENFORCEMENT_ENABLED` + `RBAC_UI_GATING` both true in `cloudbuild.yaml` (rbac.js fail-open verified). **M8-22 taken on** — founder directed "encrypt everything" (blind-index); foundation committed (`FieldCipher.blindIndex` + `migration 029` widen/bidx); scope locked; completion gated on the fleet JAC-regen. **M8-35 regression caught** — the same sync also reverted the `ClinicalOrdersRoutes` imaging wiring (missed in the batch-12 restore); AgentJAC's `9dd1f2f94` regenerated the CLINICAL_ORDERS JEO so re-wiring via generated CRUD is now unblocked (quick pass). **Fleet blocker:** JAC12 NOT-NULL create-guard regression (broken data-layer regen since ~1614) holding signup/appeals on emergency app-side workarounds (mine + AgentCICD's) and gating M8-22/M8-35 completion — see ISSUES.md `JAC12-NOTNULL-BOOLEAN-CREATE-GUARD-BREAKS-SIGNUP`. Also (non-MR8, found during suite verification) swept + fixed the systemic empty-id→22P02 crash across all 5 EDI generators + the ERA batch-update + 5 route reads → Claim Lifecycle 79/79; full suite **459/480** with the signup/appeal workarounds live.
> **2026-06-29 — M8-23 batch 2 committed (`91bce9e84`): 5 more audit call-sites on AuditWriter.** M7-11 batches 3–6 cleared MfaRoutes, ClaimDiagnosesRoutes, DenialsRoutes — migrated those 5 raw `batchCreateAUDIT_LOG` calls to `AuditWriter.write()`. Drops collidable nanoTime IDs; adds reply-check + SHA-256 integrity + Cloud Logging fallback on failure. Acknowledged bucket 65→60; UNCHECKED at ratchet ceiling 144 (unchanged — those 5 were previously marked best-effort, not in the UNCHECKED bucket). **10 files on AuditWriter total.** Remaining call-sites still held for AgentMR7's M7-11 sweep.
> **2026-06-29 — M8-35 imaging/radiology orders DONE (commit `02204601b`).** Added 6 imaging-specific fields to `clinical_orders` via idempotent `ALTER TABLE IF NOT EXISTS` (same bootstrap pattern as `RbacSchema`/`practices.rbac_enabled` — no JAC regen cycle needed; JAC agents unblocked). New schema: `ImagingOrderSchema.script` adds `imaging_modality` / `body_part` / `laterality` / `contrast_required` / `reason_for_study` / `report_impression` + a partial index on imaging orders. `ClinicalOrdersRoutes` extended: POST accepts and persists imaging fields via secondary UPDATE after batchCreate; GET batch-fetches imaging fields for all imaging-type orders in one query and merges them into the response; PUT accepts imaging field updates (including `reportImpression` on the result action). All reads/writes use sanctioned-raw `PreparedStatement` (column not in JAC-generated CRUD). Router wired with `ImagingOrderSchema.ensureSchema()` at startup. **phase0 270/270 green.** **MR81 lane now: 6/8 items done.** Remaining purely founder-gated: M8-21 Phase 5 (flip flags), M8-22 (encryption approach), M8-12 (drill window). M8-23 remaining call-sites still held for AgentMR7 M7-11 sweep.
> **2026-06-29 — M8-21 per-practice toggle already delivered (commit `2b4e66a71`, 2026-06-28); MR8Table updated.** The multi-tenant per-practice resolution that was ⛔ founder-gated is now built — each practice can independently enable RBAC from the Practices menu without waiting for a global flip. MR8Table updated to reflect this. Only Phase 5 founder flip remains.
> **2026-06-28 — M8-21 RBAC Phase 4 admin screens COMPLETE (create/delete role + user→role assignment + real multi-role union).** Built the two remaining admin screens the founder asked for: **(1) create/delete custom roles** — `POST`/`DELETE /api/rbac/admin/roles` (validated slug, reserved-key guard, cascade delete of grants+assignments) + a "+ New role" form and per-custom-role Delete button in `rbac-admin.html`; **(2) user→role assignment** — `GET /api/rbac/admin/users` (list + assigned roles, `?search=`) + `POST /api/rbac/admin/user-roles` (replace `user_roles`, sync `users.role` to the primary) + a "Users & role assignments" section with per-user role checkboxes. **Multi-role is now REAL**: `util/RbacResolver.script` computes a user's effective permissions as the **UNION** of every role in `user_roles` (cached 60s, uses only PermissionHelper's public API so the phase0 Stage-1 build stays stable); the **gate** enforces the union when a user has assignments and falls back to the unchanged single-role path otherwise (so zero behavior change until an admin assigns multi-roles); `/api/me/permissions` reports the union + a `roles[]` array so UI gating matches enforcement. **phase0 269/269 green.** All admin-only + audited + still flag-OFF. **Only remaining M8-21 work:** per-practice (multi-tenant) grant resolution — ⛔ blocked on the founder's org-vs-practice scope decision (today RBAC is one global matrix for all practices; the `org_id`-scoped rows seed but the resolver reads system defaults).
> **2026-06-28 — M8-21 RBAC keystone + full admin grid landed (enforcement is now real, still flag-OFF).** Built the **central enforcement gate** the founder asked for ("enforce without rewriting every module"): `util/RbacRouteMap.script` (a pure, unit-tested `(method,path)→permission` registry covering the core RCM resources; auth/portal/FHIR/clinical/admin/mock **exempt**) + `util/RbacGate.script` (a startup-armed singleton invoked from the **universal `JacHttp.guard` choke point** before every handler — **fail-open**, flag-gated, admin short-circuit, 403 on deny). Hermetic **phase0 build 268/268 green**; route-map logic verified **29/29** via a standalone runner + a `tests/unit/util/RbacRouteMapTest.script`. **UI gating sweep finished**: `data-rbac-op` tagged across 16 pages incl. dynamic worklist buttons, + a debounced `MutationObserver` in `rbac.js` so dynamically-rendered nodes auto-gate. **Matrix extended to the whole surface**: 4 new applications (Patient Statements, Charge Master, Medical Evidence, AR Follow-up) seeded into `rbac_feature_permissions`/`rbac_applications`/`rbac_application_actions` + default role grants (now 7 tables; 69/10/~198 features/roles/grants + 21/64 apps/actions). **Admin grid (`/ui/rbac-admin.html`) per founder**: all-access roles (`admin`/`org_admin`/`super_admin`) now show **no settings grid** (they bypass), and each editable role got **Check all / Uncheck all** buttons. Docs refreshed ([How-To](<PracticeForceOneRBACImplementationHowTo.md>) + [Matrix](<PracticeForceOneRBACMatrix.md>)). **Still OFF** — nothing denied until the founder flips `RBAC_UI_GATING`→`RBAC_SHADOW_MODE`→`RBAC_ENFORCEMENT_ENABLED`. **Left:** create-role UI, user→role-assignment UI, multi-tenant grant resolution (the `org_id` rows seed but the resolver reads system defaults).
> **2026-06-27 — M8-23 SAFE migration batch COMPLETE (7 files, ~11 audit writes on AuditWriter).** Added `AuthLoginRoutes` (all **6** login audits — failed×3/success/mfa-challenge, `4d5e1394c`), `AdminAiFlagsRoutes` + `AdminClearinghouseRoutes` + `ScriptBuilderRoutes` (`e695bdffb`) to the earlier MedicalEvidence/UatResults/RuleCandidates. **A re-scan confirms 0 remaining safe swallowed targets** — every pure-audit (no generated-CRUD primary write) swallowed AUDIT_LOG side-write is now durable + reply-checked + collision-free-id + Cloud-Logging-fallback. Remaining `batchCreateAUDIT_LOG` sites are either in files with generated-CRUD primary writes (**AgentMR7's M7-11 sweep — held** until they clear) or already correctly reply-checked (BreakGlass/AuditDisposition/RuntimeReportSnapshots). All compile standalone.
> **2026-06-26 — M8-23 migration underway (4 files on AuditWriter) + a real audit-loss BUG fixed.** Routed the swallowed best-effort AUDIT_LOG side-writes through `AuditWriter.write()` in the pure-audit files (no primary write / no AgentMR7 overlap): `MedicalEvidenceRoutes` (`7107dba07`), `UatResultsRoutes` + `RuleCandidatesRoutes` (`692911daf`), + `BreakGlassRoutes` grant-id collision fix (`7ece87fdd`). **Bug fixed:** RuleCandidates set `audit_log.id` to a UUID, but the column is **bigint** → the insert failed+was swallowed every time → the `candidate_rejected` audit was never recorded; now durable. Also flagged to @AgentMR7 that ~3 of their batch-2 `reply-unchecked` markers are on writes that ARE already checked (BreakGlass/AuditDisposition/RuntimeReportSnapshots). Remaining swallowed audit writes are in files with primary writes (AgentMR7's active M7-11 sweep) → **held** until those clear. *(Verify note: standalone compile of `692911daf` deferred — a concurrent regen has the app classes mid-rebuild; correct-by-construction + phase0 preflight is the gate.)*
> **2026-06-26 — M8-21 advanced to the founder's doorstep (kill-switch shipped + matrix decision-ready).** Added the explicit RBAC **kill-switch** `RBAC_KILL_SWITCH` (commit `2eb55660a`, zero-behavior-change while enforcement OFF — instant no-redeploy revert for the flip). Wrote a **decision-ready permission matrix** → [PracticeForceOneRBACMatrix](<PracticeForceOneRBACMatrix.md>): plain-English role table + the one real risk (un-mapped prod roles get locked out — needs a 1-query coverage check) + **5 yes/no decisions** for the founder to sign. Once signed, the flip is two env vars + a verify (no code change). *Founder: review that page + answer the 5 decisions to unblock M8-21.* (Coordination note: AgentMR7 is actively mid M7-11 audit-write sweep — batch 2 `d71ba56b2`, explicitly excluding AuditWriter — so the M8-23 call-site migration stays held until their sweep clears those files.)
> **2026-06-26 — M8-22 is a DESIGN DECISION, not a wiring task (AgentMR81 finding — needs founder).** `FieldCipher` is AES-256-GCM with a **random IV → non-deterministic ciphertext**, so you can't do equality lookups on an encrypted column. The headline P2 field, **`subscriber_id` (insurance member ID), IS used in WHERE equality** (e.g. AdminPortalUsers dedup; conceptually eligibility/claims matching) — naively encrypting it would silently break those lookups when the flag flips ON. So I did **not** wire it. **Founder options:** (1) **blind index** — store `HMAC-SHA256(subscriber_id)` in a new searchable column + GCM ciphertext for the value (the standard searchable-encryption pattern; needs a schema column + bigger build); (2) **descope searchable IDs** and encrypt only never-queried PHI (e.g. subscriber DOB/name, `group_number`) — quick, lower coverage; (3) **defer M8-22**. Recommend (1) long-term, (2) as a quick win. Awaiting the call. **M8-22 marked founder-gated.** With M8-21/M8-12 (founder), M8-35 (JAC schema-regen coord), and M8-23 migration (AgentMR7 sweep coord) also gated, the pure-additive autonomous backlog is essentially complete — loop will run lighter ticks.
> **2026-06-26 — M8-24 follow-up DONE (commit `0d6011358`).** `Patient/$everything` now also includes **Encounter** + **Coverage**, so the USCDI full-record export is Patient + AllergyIntolerance + Condition + MedicationStatement + Observation(vitals/labs) + Encounter + Coverage. Compiles. **MR81 autonomous progress so far: M8-54, M8-24 (+follow-up), M8-33, M8-23 foundation — all compile, committed.** Remaining MR81 pure-code: M8-22 encryption P2 wiring (founder needs to provision the key to flip ON); founder-gated: M8-21 (matrix), M8-12 (drill window). M8-35 imaging deferred pending JAC schema-regen coordination. Source increments fold into the next deploy (not deploying solo).
> **2026-06-26 — M8-23/M7-72 foundation DONE (durable AuditWriter, commit `aa209c8e2`).** Net-new `util/AuditWriter.script`: collision-free monotonic bigint id (replaces nanoTime — caught + fixed a 60%-collision bug and a null-getter throw in test), reply-check, and a `[AUDIT-FALLBACK]` Cloud-Logging line so audit events are never silently dropped, + per-record SHA-256 integrity hash. Touches ZERO existing call sites (no collision with AgentMR7's M7-11 sweep). *Remaining:* migrate the ~69 in-line audit writes to `AuditWriter.write()`, per-domain, sequenced after MR7's sweep (coordinated). Next MR81: M8-35 (imaging) / M8-24 follow-up (Encounter+Coverage in `$everything`).
> **2026-06-26 — M8-33 DONE (Rx refill workflow, commit `93da4d859`).** eRx PUT handler extended (was cancel-only) with pharmacy refill: `request-refill` (inbound RxRenewalRequest → refill_requested), `approve-refill` (outbound RxRenewalResponse(Approved) + transmits a renewed prescription → refill_approved), `deny-refill` (RxRenewalResponse(Denied) → refill_denied) — each reply-checked + audited in one transaction. Compiles. Next MR81: M8-23 (durable audit = M7-72).
> **2026-06-26 — MR81 delivering autonomously (founder: "work 24x7, only surface true blockers").** **M8-54 DONE** (drug-safety screening seam, commit `64e66f286`). **M8-24 DONE** (USCDI export, commit `9e5e10cad`): three new FHIR R4 resources — **AllergyIntolerance** (PATIENT_ALLERGIES), **Condition** (PATIENT_PROBLEMS/ICD-10-CM), **MedicationStatement** (PATIENT_MEDICATIONS/RxNorm) — each read+`?patient=` search, org-scoped, generated-CRUD; plus **`Patient/{id}/$everything`** (one Bundle: Patient + the three + Observation vitals/labs); CapabilityStatement + router updated; all six modules compile. *Follow-up:* fold Encounter+Coverage into `$everything`. Next MR81 (no vendor gate): M8-33 (Rx refill), then M8-23 (durable audit = M7-72).
> **2026-06-26 — FOUNDER set a WAVE sequencing for MR8 (see "Wave plan" below).** Founder: *"I'd divide MR8 into four waves."* **Wave 1 — Revenue Activation:** M8-1, M8-2, M8-3, M8-4, M8-5, M8-6 (*"transform PFO from a demo into a revenue-producing system"*). **Wave 2 — Customer Proof:** M8-10, M8-11, M8-12, M8-13 (*"where companies either become real or stay interesting… not glamorous"*). Waves 3–4 not yet detailed by founder — AgentMR81 proposed a draft (cert/security, then clinical/engagement) pending his confirmation. **First delivery already landed:** M8-54 server-side drug-safety screening seam (drug-allergy + drug-drug) — net-new, flag-gated OFF, logic-verified, committed `64e66f286`. (Note: M8-54 sits in the proposed Wave 4; built first only because it was 100% net-new/zero-collision while waves get sequenced.)
> **2026-06-26 — MR8 ACTIVATED. Founder selected 16 rows; AgentMR81 + AgentMR82 executing.** Picks (☑): **M8-2, M8-3, M8-4, M8-6, M8-12, M8-21, M8-22, M8-23, M8-24, M8-33, M8-35, M8-40, M8-41, M8-50, M8-54, M8-55.** Code-level current-state assessment shows MR8 is a **maturation** effort — almost every pick already has a built seam (eligibility/ERA/FieldCipher/AsyncAuditQueue/Fhir*/Erx/PermissionHelper); the work is taking each from simulated/partial → production-grade. **Owner split (theme-based, minimizes file collision):** **MR81** = M8-21, M8-22, M8-23, M8-24, M8-33, M8-35, M8-54, M8-12. **MR82** = M8-2, M8-3, M8-4, M8-6, M8-40, M8-41, M8-50, M8-55. Founder-gated (not pure builds): **M8-21** (needs permission-matrix sign-off before flag flip), **M8-22** (needs `FIELD_ENCRYPTION_KEY` provisioned), **M8-12** (needs a drill window). MR81 starting with **M8-54** (net-new, schema-free) then **M8-24**. See AGENTS.md 2026-06-26 AgentMR81 post for the full per-item gap analysis.
> **2026-06-26 — MR8 OPENED as a founder pick-list (from the ECW deep dive).** Founder: *"plan how we fill the most critical gaps"* + *"make each enhancement easy to understand from a user's perspective so I can choose."* Every row is benefit-first with a checkbox. Nothing started — awaiting selections.

---

## 🌊 Wave plan (founder sequencing — 2026-06-26)

Founder framing: **four waves.** Waves 1–2 are his words; Waves 3–4 are AgentMR81's **proposal** awaiting founder confirmation. The 16 execution picks (☑) and the MR81/MR82 owner split map onto the waves as follows. **Vendor/founder-gated** = needs a contract or a founder decision before code can go live (the seam may already be built).

| Wave | Theme | Items | Owner(s) | Gate |
|---|---|---|---|---|
| **1** | **Revenue Activation** (demo → revenue) | M8-1 clearinghouse · M8-2 eligibility · M8-3 ERA · M8-4 live AI denial · M8-5 payments · M8-6 statements | **MR82** (M8-2/3/4/6) | **M8-1** (clearinghouse contract) + **M8-5** (payment processor) are vendor-gated and the unlock for the rest. Do **M7-10** (test-tools-out-of-prod) first. |
| **2** | **Customer Proof** (become real) | M8-10 first production practice · M8-11 performance · M8-12 disaster-recovery drill · M8-13 founder independence | **MR81** (M8-12) | M8-10 = a willing practice (founder); M8-11 = MR7 M7-14 (unassigned); **M8-13 ≈ MR7 M7-71 = ✅ already DONE** (ops runbook shipped; only the human 2nd-operator drill remains); M8-12 = MR7 M7-31 (founder picks a drill window). |
| **3** *(proposed)* | **Buyer Checklist** (cert & security) | M8-20 ONC cert path · M8-21 RBAC on · M8-22 field encryption · M8-23 durable audit · M8-24 USCDI export | **MR81** (M8-21/22/23/24) | M8-20 = founder cost/timeline call; **M8-21 = MR7 M7-17** (founder permission-matrix sign-off, then flip); M8-22 = `FIELD_ENCRYPTION_KEY` provisioning; **M8-23 = MR7 M7-72** (HIPAA audit durability — pure code; coordinate w/ MR7's M7-11 ratchet). |
| **4** *(proposed)* | **Clinical & Engagement** | M8-30→35 e-Rx/labs/imaging · M8-40 reminders · M8-41 self-scheduling · M8-50/54/55 | **MR81** (M8-33/35/54) · **MR82** (M8-40/41/50/55) | M8-30/34 = Surescripts/lab contracts; M8-40 = Twilio/SendGrid; M8-50/54 = partner. **M8-54 already delivered** (seam, flag-OFF). |

**Note on the 16 picks vs the waves:** the founder's Wave 1 reintroduces **M8-1** and **M8-5** (not in the original 16). Those are vendor contracts — flagged here so the decision is deliberate. Everything else in the 16 already has an owner above. *AgentMR81: confirm or revise Waves 3–4 and whether M8-1/M8-5 join the active 16.*

---

## How to read the priority

Think of it as four questions, in order:

1. **Can we recover real money yet?** → Connect one billing network (§1). *Biggest payoff, smallest effort.*
2. **Can we prove it works at a real busy practice?** → One multi-doctor site at volume (§2).
3. **Will buyers’ checklists let them say yes?** → Certification + security toggles (§3).
4. **What stops us losing deals on missing basics?** → e-prescribing, labs, patient reminders (§4–§5). *Plus a “don’t-build-this-yet” list (§6) so we stay focused.*

**My recommended first 3 (highest value, lowest effort):** **M8-1**, **M8-2**, **M8-3** — connect one billing clearinghouse. This single vendor contract turns the whole AI billing engine from a demo into live recovered revenue.

---

## §1 — Turn on real billing (the money-maker) 💰⭐

*Today the smart-billing engine (catches denials, writes appeals, scores risk, learns payer rules) is **fully built but practicing on fake data**. Connect one billing network and it starts working on real claims. This is the highest-value, lowest-effort part of the whole plan.*

| Pick | # | Enhancement | What you get (plain English) | Size · What it takes |
|:--:|---|---|---|---|
| ☐ | **M8-1** | **Connect a billing clearinghouse (send real claims)** 💰 | Your claims actually go out the door to insurers electronically, and you see their acknowledgements come back on the board — instead of the current pretend submission. | **M** · pick + sign one clearinghouse (Availity / Optum / Waystar / Office Ally). *(Do the M7-10 "keep test tools out of production" step first.)* |
| ☑ | **M8-2** | **Real-time insurance eligibility checks** 💰 | When you schedule or check in a patient, the system instantly confirms their coverage is active and shows copay/deductible — so you stop billing dead coverage and surprising patients. | **S–M** · rides on M8-1's connection. |
| ☑ | **M8-3** | **Automatic payment posting from insurers (ERA)** 💰 | When an insurer pays, the money and adjustments post to the right claims automatically, and any denials drop straight into the work queue — no manual keying. | **M** · rides on M8-1's connection. |
| ☑ | **M8-4** | **Switch on the AI denial-recovery loop (live)** 💰⭐ | A denied claim automatically gets an AI-written explanation of *why*, a drafted appeal letter ready to send, and a risk score — every action logged so you can trust it. **This is the feature that sells the product.** | **S** · just needs M8-1/2/3 live + AI toggles on (already built). |
| ☐ | **M8-5** | **Take real patient payments (card / ACH)** 💰 | Patients can actually pay their balance online and at the desk; it posts to their account immediately. (Today it's simulated.) | **M** · pick + sign a payment processor (e.g. Stripe). |
| ☑ | **M8-6** | **Send real patient statements** | Outstanding balances go out as real e-statements / printed statements with a pay link, instead of living only in a report. | **M** · needs a statement/mail vendor (+ M8-5 for the pay link). |

---

## §2 — Prove it at a real, busy practice 🧱

*"It only runs at one small clinic" is the #1 reason a serious buyer hesitates. This section is about putting it in front of real volume and making sure it stays fast and safe.*

| Pick | # | Enhancement | What you get (plain English) | Size · What it takes |
|:--:|---|---|---|---|
| ☐ | **M8-10** | **Go live at one multi-provider practice at volume** 🧱 | A real reference customer: several providers, a full month of real claims and visits, with proof that nothing broke. This is what makes the sales story credible. | **L** · a willing practice + your time onboarding. |
| ☐ | **M8-11** | **Make it stay fast under load** | Dashboards and work queues keep loading instantly even with thousands of claims/patients (fixes spots that currently load everything at once). | **M** · best done under M8-10's real data. (= MR7 M7-14.) |
| ☑ | **M8-12** | **Backup + practiced "restore from disaster" drill** 🧱 | Confidence that if something goes wrong, you can get the data back — proven by an actual rehearsal, not just a setting. | **S** · pick a window to run the drill. (= MR7 M7-31.) |
| ☐ | **M8-13** | **Reduce "only the founder can run it" risk** | Written runbooks + toolchain docs so a second person can deploy and recover the system without you. Protects the business's value. | **M** · ongoing. (= MR7 M7-70/71.) |

---

## §3 — Clear the buyer's checklist (certification & security) 🧱

*Some buyers (especially anyone in insurance/quality programs) won't even look without these. Several are just toggles that are built but turned off.*

| Pick | # | Enhancement | What you get (plain English) | Size · What it takes |
|:--:|---|---|---|---|
| ☐ | **M8-20** | **Government EHR certification (ONC/Cures) path** 🧱 | The official certification badge that lets practices use PFO for insurance/quality programs and satisfies procurement. First step = a costed plan + the first requirement closed. | **XL** · your decision on cost/timeline + which customer segment demands it. |
| ☑ | **M8-21** | **Turn on role-based permissions in production** 🧱 | Staff only see/do what their role allows (biller vs front-desk vs provider). The framework's built — this switches it on with a safety net. | **S–M** · you sign off the permission matrix. (= MR7 M7-17.) |
| ☑ | **M8-22** | **Extra encryption of the most sensitive data** | Stronger at-rest protection of the most sensitive patient fields — useful for security reviews and BAAs. | **M** · a design decision on scope. |
| ☑ | **M8-23** | **Bulletproof audit trail (never drops a record)** 🧱 | Every access to patient data is recorded reliably and can't be quietly lost — what HIPAA audits expect. | **M** · builds on the M8-21 work. (= MR7 M7-72.) |
| ☑ | **M8-24** | **Standards-based records export (USCDI / C-CDA / FHIR)** | Patients and other systems can get a clean, standard copy of the record — required for certification and interoperability. | **M** · scoped by M8-20. |

---

## §4 — Clinical basics every practice expects 🧱

*If a practice uses PFO as their main system, these are table-stakes. Like §1, they're built as ready-to-connect seams — connecting them is integration, not a rebuild.*

| Pick | # | Enhancement | What you get (plain English) | Size · What it takes |
|:--:|---|---|---|---|
| ☐ | **M8-30** | **Real e-prescribing (send Rx to pharmacies)** 🧱 | Providers send prescriptions electronically to real pharmacies through the national network (Surescripts), instead of the current simulation. | **L** · Surescripts certification + contract. |
| ☐ | **M8-31** | **E-prescribe controlled substances (EPCS)** | Legally send controlled-substance prescriptions electronically (needs identity-proofing + DEA-compliant security). | **M** · after M8-30 + certification. |
| ☐ | **M8-32** | **Controlled-substance database check (PDMP)** | Before prescribing a controlled drug, the provider sees the state's prescription history — a safety/compliance must in many states. | **M** · after M8-30 + state access. |
| ☑ | **M8-33** | **Prescription refill handling** | Pharmacy refill requests come in, the provider approves, and it's sent back — a normal daily workflow that's not built yet. | **M** · after M8-30. |
| ☐ | **M8-34** | **Real lab orders & results** 🧱 | Order labs to LabCorp/Quest and get structured results back onto the chart automatically (today it's simulated). | **M–L** · a lab interface contract. |
| ☑ | **M8-35** | **Imaging/radiology orders** | Order X-rays/MRIs and receive the report back on the chart. | **M** · after the lab pattern (M8-34). |

---

## §5 — Keep patients engaged (compete with healow) 🧱🤝

*eClinicalWorks' patient app is a big selling point. We don't need to match all of it — just the pieces that **fill the schedule and cut no-shows**, which compound our workflow edge. Video and a phone app are better to buy/partner than build.*

| Pick | # | Enhancement | What you get (plain English) | Size · What it takes |
|:--:|---|---|---|---|
| ☑ | **M8-40** | **Appointment reminders (text / email / voice)** 💰 | Automatic reminders cut no-shows (each no-show is lost revenue) and let patients confirm or reschedule with a tap. High return for low effort. | **S–M** · a messaging vendor (Twilio/SendGrid). |
| ☑ | **M8-41** | **Patients book their own appointments online** 💰 | Patients self-schedule into real open slots; the booking lands on your workflow board automatically — fewer phone calls, fuller schedule. | **M** · you set the booking rules/guardrails. |
| ☐ | **M8-42** | **Secure two-way messaging with patients** | Patients and staff message securely through the portal, and each message becomes a tracked task so nothing gets dropped. | **M** · builds on the reliable-audit work (M8-23). |
| ☐ | **M8-43** | **Release lab results to the patient portal** | Providers release results to patients online (instead of phone tag), on the provider's terms. | **M** · after real labs (M8-34). |
| ☐ | **M8-44** | **Telehealth video visits** 🤝 | Launch a video visit right from the appointment. **Recommend partnering** a video provider (Zoom/Twilio/Doxy) rather than building. | **M** · your build-vs-partner call. |
| ☐ | **M8-45** | **Patient phone app** 🤝⏸ | A native iOS/Android app. **Recommend defer** — a phone-friendly web version covers most of it far cheaper. | **L** · strategy decision first. |

---

## §6 — Recommend NOT building yet (so we stay focused) ⏸

*These are real gaps, but chasing them now trades our winning edge for a losing race against a 25-year incumbent. Listed so the choice is **deliberate, not forgotten.** Default recommendation: skip for now.*

| Pick | # | Enhancement | What you get (plain English) | Recommendation |
|:--:|---|---|---|---|
| ☑ | **M8-50** | **AI ambient scribe (auto-writes the visit note)** | Listens to the visit and drafts the note (eCW's big "Sunoh.ai" feature). | 🤝 **Partner**, don't build — our edge is billing-AI, not note-AI. Revisit after §1. |
| ☐ | **M8-51** | **Population health / quality reporting (MIPS/HEDIS)** | Tracks care gaps and quality measures across patient populations. | ⏸ **Defer** — matters for value-based-care buyers, who aren't our first target. |
| ☐ | **M8-52** | **Hospital/regional record sharing (HIE)** | Pulls/pushes records to Carequality/CommonWell networks. | ⏸ **Defer** to after certification (M8-20). |
| ☐ | **M8-53** | **Deep specialty content (order sets, growth charts, etc.)** | Specialty-specific clinical templates and tools. | ⏸ **Build per customer** — only what the first real specialty client needs. |
| ☑ | **M8-54** | **Drug-interaction safety alerts (server-side)** | Automatic drug-drug / drug-allergy warnings from a licensed drug database. | 🤝 **Partner a drug database** when e-prescribing (M8-30) goes live. |
| ☑ | **M8-55** | **Custom report builder / scheduled BI** | Let users build their own custom reports and dashboards. | ⏸ **Defer** — current dashboards cover the wedge. |

---

## At-a-glance: my recommended starting picks

If you want a fast, high-ROI path, choose these and we'll sequence them:

1. ☐ **M8-1 + M8-2 + M8-3** — connect one billing network *(turns the AI billing engine into real money)*
2. ☐ **M8-4** — switch on live AI denial-recovery *(the feature that sells)*
3. ☐ **M8-10** — prove it at one busy multi-doctor practice
4. ☐ **M8-21** — turn on role permissions *(quick, clears a security checkbox)*
5. ☐ **M8-40** — appointment reminders *(quick, cuts no-shows = money)*
6. ☐ **M8-30** — real e-prescribing *(table-stakes for clinical use)*
7. ☐ **M8-20** — start the certification plan *(long, so start early)*

> **One caution before §1 goes live:** do the MR7 "separate test tools from the real system" step (M7-10) first — we don't want real billing/payment credentials in a system that still carries test/reset tools. It's a small prerequisite, not a blocker.

---

*Mark the rows you want (☑) and tell me — I'll convert your picks into an active, sequenced build plan with owners and deploy steps. Derived from [PracticeForceOneECWDeepDive](<PracticeForceOneECWDeepDive.md>); hardening dependencies live in [MR7](<PracticeForceOneMR7Table.md>).*

## Review Epilog — 2026-07-24

- MR8 confirmed FORMALLY CLOSED 2026-07-07. 16/16 picks engineering-complete + production-verified. Plaintext encryption debt retired (178/178 backfilled, DB proof on build 1683).
- MR8 Phase-II charter (Security Engine, RBAC, Auditing, Staged deployments, Package promotion, Tenant isolation, Config governance) is now AgentMR8's active lane. Security Engine `ProtectedAccess.mayAccess()` live. `/api/audit` + `/api/audit/summary` live.
- All open MR8 items pushed to MR9 (§8 M9-80..88 + stitched IDs). MR9 is the live backlog; this table is the activation-detail reference.
- Current build: 1943, gate 251/251 GREEN. Field encryption ON (AES-256-GCM), RBAC enforcement ON since 1534, MFA live (portal 14/14 validated).
